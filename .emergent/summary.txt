<analysis>
The trajectory details the evolution of the Twelvr adaptive learning platform, moving from an initial MVP state to a refined, skill-based adaptive system. The AI engineer first summarized existing functionalities, addressing initial product requirements like MCQ storage, adaptive-only sessions, payment, and dashboard UI. A critical phase involved the user providing an assessment of perceived bugs, which the AI engineer largely refuted, confirming the existing system's stability. Subsequently, a major new feature, the Coverage System with AskTwelvr, was introduced, shifting the core adaptive logic from difficulty-based to skill-based. This new feature's specification underwent extensive iterative refinement between the user and the AI engineer, solidifying an all-in no-fallback architecture. The final plan incorporates numerous technical fixes and optimizations, ensuring robustness, performance, and data integrity.
</analysis>
<product_requirements>
The Twelvr application is an adaptive CAT preparation platform that personalizes question sets using LLMs. Initial requirements included MCQ simplification, removal of legacy sessions for an adaptive-only system, payment system cleanup (Razorpay, Pro Lite removal, tier definitions), privileged user and free-tier logic, referral system verification, IST timezone conversion, and a collapsible student dashboard UI. The current work involved transitioning from a difficulty-based system to a **skill-based coverage adaptive learning system**. This includes:
1.  **Anchor Writer v1**: An LLM job to populate  (1-2 precise skill phrases). This is a mandatory precondition, integrated into the existing  gate.
2.  **Single Session Loop**: Exactly two LLM calls per session.
    *   **Post-session Summarizer**: Updates a  (user skills, status: weak/moderate/strong).
    *   **Pre-session Planner**: Generates a  (priority skills, borrow order, PYQ intent).
3.  **Deterministic Picker**: Backend selects 12 questions (3 Easy, 6 Medium, 3 Hard) based on the recipe, enforcing PYQ minima, and applying caps with band-aware backfill.
4.  **AskTwelvr**: Inline chat feature for step-by-step solution explanations and practice.
</product_requirements>
<key_technical_concepts>
- Full-stack: React (Frontend), FastAPI (Backend), PostgreSQL
- Adaptive Learning: LLM-powered planning (GPT-4o-mini), deterministic kernels
- Database: SQLAlchemy, PostgreSQL (migrations, indexes, JSONB arrays, unique constraints, NOT NULL)
- LLM Integration: Google Gemini, OpenAI GPT-4o-mini (via )
- Payment Gateway: Razorpay
- Timezones:  for IST conversion
- Core Adaptive Concepts: Anchors (skill phrases), Learner Notebook, Inventory Digest, Coverage Ledger.
</key_technical_concepts>
<code_architecture>

-   ** (NEW)**: Multiple SQL migrations to add new columns (, , , , , , ), new tables (, ), unique constraints (), composite indexes (, ), normalize  to lowercase and enforce  and  constraints.
    -   **Importance**: Foundation for the new coverage system, ensuring data integrity and query performance.
-   ** (NEW)**: LLM service to generate and normalize skill anchors for questions.
    -   **Importance**: Populates the  field, a core component of the coverage system's eligibility logic. Anchors are normalized (lowercase, deduplicated, max 2).
-   ** (NEW)**: Integrates anchor generation and validation into the existing quality verification pipeline.
    -   **Importance**: Ensures  only if a question has valid anchors, making anchors mandatory for eligibility.
-   ** (NEW)**: Manages a user's Learner Notebook (skill statuses).
    -   **Importance**: Stores and retrieves a user's latest skill proficiencies. Includes a backup mechanism to .
-   ** (NEW)**: LLM service that processes session attempts and updates the Learner Notebook.
    -   **Importance**: Provides AI-driven insights into user skill status (weak/moderate/strong) based on session performance. Runs exactly once per session after 12 attempts.
-   ** (NEW)**: Builds a summary of eligible questions grouped by skill view and difficulty band.
    -   **Importance**: Provides the Planner LLM with statistics on available questions for selection. Handles no notebook by treating all eligible questions as Moderate.
-   ** (NEW)**: LLM service that generates a Selection Recipe based on the Learner Notebook and Inventory Digest.
    -   **Importance**: Creates a high-level plan for question selection, specifying priority skills and borrowing logic. Includes a default recipe fallback for LLM failures.
-   ** (NEW)**: Deterministic service that selects exactly 12 questions based on the Planner's recipe.
    -   **Importance**: Implements the core adaptive logic: PYQ-first, 4-tier priority (skill match, PYQ score, served count, seeded hash), band-aware backfill (preserves 3-6-3), pack-level PYQ top-up, and caps (≤2 per subcategory|type). Uses  for stable seeding and preloads served counts for performance.
-   ** (NEW)**: Orchestrates the entire session planning flow using the new coverage services.
    -   **Importance**: Main entry point for generating a coverage-based session, saving the final pack with audit details. Normalizes pack items to use a canonical uid=0(root) gid=0(root) groups=0(root) field.
-   ****: Updated API endpoint for  and .
    -   **Importance**:  now triggers the .  is made idempotent, atomic (transactional), and updates  based on the served pack, relying only on  for queries.
-   ****: Updated for attempt logging.
    -   **Importance**: Snapshots  from the *served pack* (not live question), uses canonical uid=0(root) gid=0(root) groups=0(root) lookup, and triggers the  exactly once per session (after 12 attempts, with idempotency).
-   ****: Updated to emit metrics for the coverage system.
    -   **Importance**: Tracks new metrics like , , ,  details (shape, PYQ, borrow), and . Audits are recomputed from the *final pack*.
-   ** (NEW)**: Comprehensive script for pre-flight validation.
    -   **Importance**: Ensures system readiness by testing eligible count, LLM reliability, pack generation consistency (size, shape, PYQ, audit accuracy), one-time summarizer, and mark-served idempotency.
</code_architecture>
<pending_tasks>
The core pending task is the **full implementation of the Twelvr Coverage System** as detailed in the final implementation plan. This includes:
- Executing all database migrations.
- Implementing the Enhanced Quality Verifier and running it to populate anchors.
- Building and integrating all new backend services: Learner Notebook, Coverage Summarizer, Inventory Digest, Coverage Planner, Coverage Selector, and Coverage Pipeline.
- Updating API endpoints (, , ) for coverage-only functionality.
- Implementing minimal telemetry for the new system.
- Developing frontend components for AskTwelvr (not detailed in the backend plan).
- Conducting comprehensive pre-flight testing and smoke tests.
</pending_tasks>
<current_work>
Immediately before the request for the new Coverage System, the previous AI engineer successfully stabilized the existing Twelvr application to a 100% functional state. This involved resolving several critical issues:
-   **Security Fix**: Correcting the  user's unintended admin access by reverting their  status to , ensuring  is the *only* admin.
-   **Student Dashboard Fix**: Rectifying an infinite  loop in  and an authentication race condition in  that caused auto-logout and disappearing dashboard data. The dashboard now correctly displays canonical CAT taxonomy with collapsible subcategories by fetching data from .
-   **Payment Integration**: Resolving errors in  related to Razorpay response handling (correcting  to ) and removing an incorrect  check, allowing the Razorpay modal to open and the payment flow to function.
-   **MCQ Formatting**: The AI engineer also analyzed user concerns about MCQ JSON formatting (e.g., Unicode escape sequences) and confirmed that the system correctly stores and parses these, rendering them properly for students and handling edge cases gracefully.

The system was verified to have accurate difficulty band ranges, a compatible pack schema, and correct answer field usage, effectively refuting earlier user-identified broken points. The current session planner uses a deterministic selector and an LLM for reordering, with a backup plan.
</current_work>
<optional_next_step>
The next step is to begin implementing the FINAL production-ready plan for the Twelvr Coverage System, starting with Day 1 tasks: database migrations and enhancing the quality verification system.

Ask user to paste the implementation plan in the next session and adhere to that while implementing.
</optional_next_step>
